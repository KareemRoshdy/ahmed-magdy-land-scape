export type NavLinkType = {
  name: string;
  href: string;
};

export type servicesListType = {
  id: number;
  image: string;
  title: string;
  description: string;
  images: { id: number; src: string }[];
};
